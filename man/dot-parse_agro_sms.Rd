% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sms.R
\name{.parse_agro_sms}
\alias{.parse_agro_sms}
\title{Parse a single agrometeorological SMS into a long tibble}
\usage{
.parse_agro_sms(txt)
}
\arguments{
\item{txt}{\code{character(1)}. A single raw SMS string with newline-separated
lines.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{eg_gh_id}, \code{year}, \code{month},
\code{day}, \code{time}, \code{eg_el_abbreviation}, \code{value}, \code{flag}. One row per
variable (15 rows for a complete message). Returns a single-row
all-\code{NA} tibble when parsing fails.
}
\description{
Internal helper. Parses a multi-line agrometeorological SMS message into a
tidy long-format tibble with one row per observed variable. Returns a
single-row all-\code{NA} tibble for any message that fails validation or cannot
be parsed.
}
\details{
The SMS format is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{STATION-NAME
DD-MM-YYYY
Tn= 305
Tx= 438
...
RA= NT
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=to_station_id]{to_station_id()}}, \code{\link[=to_element_id]{to_element_id()}}, \code{\link[=parse_sms]{parse_sms()}}
}
\keyword{internal}
